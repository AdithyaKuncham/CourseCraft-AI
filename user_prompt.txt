<USER_REQUEST>
Implement the complete Outcome Mapping feature for CourseCraft AI.
This covers Course Outcome (CO) generation with Bloom's taxonomy
and CO-PO Matrix generation. Plug into the existing codebase
structure without changing anything outside the files mentioned.

=== FOLDER STRUCTURE ADDITIONS ===

backend/src/
  models/
    OutcomeMapping.js          ← create
  routes/
    outcome.routes.js          ← create
  controllers/
    outcome.controller.js      ← create
  services/
    groq.service.js            ← update: add generateCourseOutcomes()
                                          and generateCOPOMatrix()
  middleware/
    validate.middleware.js     ← update: add validateOutcomeGenerate()
  app.js                       ← update: mount outcome routes

frontend/src/
  pages/
    OutcomePage.jsx            ← create
  components/
    outcome/
      OutcomeInputForm.jsx     ← create
      OutcomeSourceSelector.jsx← create
      SyllabusInputPanel.jsx   ← create
      POInputPanel.jsx         ← create
      CourseOutcomeResult.jsx  ← create
      COPOMatrixResult.jsx     ← create
  hooks/
    useOutcomes.js             ← create
  utils/
    pdfExtractor.js            ← create (client-side PDF text extraction)

=== BACKEND ===

--- backend/src/models/OutcomeMapping.js ---

Mongoose schema:

  userId        : ObjectId, ref: "User", required
  programId     : ObjectId, ref: "Program", default: null
  courseId      : ObjectId, ref: "Course", default: null

  sourceType    : String, required
                  enum: ["existing_curriculum", "existing_course", "manual"]

  courseName    : String, required, trim
  courseCode    : String, required, trim
  syllabusText  : String, required
                  ← extracted text only, never raw PDF

  programOutcomes: [
    {
      poNumber  : Number,
      statement : String
    }
  ]

  generatedOutcomes: {

    courseOutcomes: [
      {
        coNumber      : Number,
        statement     : String,
        bloomsLevel   : String,
      
<truncated 25430 bytes>
/api/courses/my-courses"),
    axiosInstance.get("/api/outcomes/my-mappings")
  ])

  const mappings = mappingsRes.data.mappings.map(m => ({
    ...m,
    itemType: "outcome"
  }))

  Combine all three, sort by createdAt desc, slice to 5.

In render, add outcome item type:
  if item.itemType === "outcome":
    Icon: BarChart2 in amber circle
    Title: item.courseName
    Subtitle: item.courseCode + " · Outcome Mapping"

In handleActivityClick:
  if item.itemType === "outcome":
    navigate(ROUTES.OUTCOMES, {
      state: { mappingId: item._id }
    })

Update stats card:
  "Outcomes Mapped" value: fetch from /api/curriculum/stats
  (already returns outcomes count after backend update above)

=== ROUTES UPDATE ===

In frontend/src/constants/routes.js:
  OUTCOMES is already defined as "/dashboard/outcomes" — no change.

In frontend/src/App.jsx:
  Replace the outcomes placeholder with:
    /dashboard/outcomes → <OutcomePage />

=== DO NOT CHANGE ===
  Curriculum generation logic
  Course generation logic
  Authentication logic
  Any existing model schemas
  Folder/file naming conventions
  Groq client initialization
  Credit computation utilities
  Any existing middleware except adding to validate.middleware.js
  App.jsx routes except outcomes route
  Sidebar nav labels or structure
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-06-09T13:42:18+05:30.

The user's current state is as follows:
Active Document: c:\Users\ASUS\OneDrive\Desktop\CODE\CourseCraft_AI\frontend\src\pages\CoursePage.jsx (LANGUAGE_JAVASCRIPT)
Cursor is on line: 1
Other open documents:
- c:\Users\ASUS\OneDrive\Desktop\CODE\CourseCraft_AI\frontend\src\pages\CoursePage.jsx (LANGUAGE_JAVASCRIPT)
</ADDITIONAL_METADATA>
<USER_SETTINGS_CHANGE>
The user changed setting `Model Selection` from Gemini 3.5 Flash (Medium) to Gemini 3.1 Pro (High). No need to comment on this change if the user doesn't ask about it. If reporting what model you are, please use a human readable name instead of the exact string.
</USER_SETTINGS_CHANGE>